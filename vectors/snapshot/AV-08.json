{
  "id": "AV-08",
  "family": "delegated-authority",
  "profile": "snapshot",
  "invariant": "INV-02",
  "description": "Sequence replay: a replayed delegation DO carries an already-consumed chain_seq; the audit chain's Phase-1 continuity check detects the replay deterministically (no wall-clock, per E9).",
  "rule": {
    "id": "INV-02_sequence_replay",
    "name": "INV-02_sequence_replay",
    "description": "A delegation DO must not carry an already-consumed chain_seq (no sequence replay)",
    "category": "security",
    "conditions": [
      { "expr": { "lte": [{ "field": "replayed.seq" }, { "field": "chain.last_consumed_seq" }] } }
    ],
    "conditionLogic": "AND",
    "action": {
      "decision": "DENY",
      "reason": "replayed delegation carries an already-consumed chain_seq (INV-02)"
    },
    "priority": 1,
    "enabled": true
  },
  "attack": { "replayed.seq": 5, "chain.last_consumed_seq": 10 },
  "legal": { "replayed.seq": 11, "chain.last_consumed_seq": 10 },
  "attribution": {
    "originating_task": "T-delegate-0908",
    "authority_provenance": "P→A→B",
    "violation_hop": "A→B",
    "requested_action": "accept(T, replayed-delegation)",
    "effective_authority": "chain_seq must be unconsumed",
    "reason": "SEQUENCE_REPLAY"
  }
}
